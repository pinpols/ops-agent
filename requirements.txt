# 核心运行时依赖(单一真源 = pyproject.toml [project].dependencies)。
# 此文件仅列“只跑核心”所需;graph / observability / dev 是 pyproject 的可选 extras,
# 不在此重复(避免与 pyproject 漂移)。精确复现用 requirements.lock。
#
# 推荐安装(含开发/测试/LangGraph):  pip install -e ".[dev,graph]"
# 仅核心:                          pip install -r requirements.txt
# 完全锁定复现:                    pip install -r requirements.lock
anthropic>=0.69,<1
pydantic>=2.9
python-dotenv>=1.0
psycopg[binary]>=3.2
