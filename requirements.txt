# ⚠️ 非权威依赖清单(P3):仅供 `pip install -r requirements.txt` 跑核心运行时的最小子集,
# 版本可能落后 —— 权威锁定文件是 requirements.lock(uv pip compile 生成,含 hash,CI 用)。
# 开发/测试请用 `pip install -e ".[dev]"`;升级依赖时两个文件都要同步。
annotated-types==0.7.0
anthropic==0.39.0
certifi==2024.12.14
charset-normalizer==3.4.1
click==8.1.7
distro==1.9.0
idna==3.10
pydantic==2.9.2
pydantic-core==2.23.4
python-dotenv==1.0.1
pyyaml==6.0.2
requests==2.32.3
typing-extensions==4.12.2
urllib3==2.3.0
